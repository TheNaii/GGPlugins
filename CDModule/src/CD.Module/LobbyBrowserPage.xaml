<UserControl x:Class="CD.Module.LobbyBrowserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="8"
                                Background="#7C3AED"
                                Padding="20,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#6D28D9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#374151"/>
                                <Setter Property="Foreground" Value="#6B7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LobbyCard" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="28,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <DockPanel>
                <TextBlock Text="Lobbies"
                           FontSize="20" FontWeight="Bold"/>
                <TextBlock DockPanel.Dock="Right"
                           x:Name="PlayerInfo"
                           Opacity="0.5" VerticalAlignment="Center"
                           HorizontalAlignment="Right"/>
            </DockPanel>
            <TextBlock Text="Join a lobby or create your own."
                       Opacity="0.5" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Create lobby bar -->
        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="LobbyNameBox"
                     Grid.Column="0" Height="36"
                     FontSize="13" MaxLength="40"
                     Padding="10,0"
                     VerticalContentAlignment="Center"
                     Text=""/>

            <Button Grid.Column="2" Style="{StaticResource AccentButton}"
                    Content="Create Lobby"
                    Click="Create_Click"/>

            <Button Grid.Column="4" Style="{StaticResource AccentButton}"
                    Content="⟳ Refresh"
                    Click="Refresh_Click"/>
        </Grid>

        <!-- Lobby list -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="LobbyList"/>
        </ScrollViewer>

        <!-- Status -->
        <TextBlock Grid.Row="3" x:Name="StatusText"
                   FontSize="11" Opacity="0.5"
                   Margin="0,8,0,0"/>
    </Grid>
</UserControl>
